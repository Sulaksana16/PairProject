<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('./navbar.ejs')%>
    <h1>Tournament List</h1>
    <table border>
        <tr>
            <th>Tournament</th>
            <th>Organizer</th>
            <th>Quota</th>
            <th>Prize Total</th>
            <th>Location</th>
            <th>Date</th>
            <th>Register</th>
        </tr>
        <% for (let i = 0; i < tournaments.length; i++){%>
            <tr>
                <td><%= tournaments[i].name %></td>
                <td><%= tournaments[i].organizer %></td>
                <td><%= tournaments[i].quota %></td>
                <td>IDR <%= tournaments[i].prize %></td>
                <td><%= tournaments[i].location %></td>
                <td><%= tournaments[i].date %></td>
                <td>
                    <a href="/tournaments/<%= user.id%>/register/<%= tournaments[i].id %>"><button>register</button></a>
                    <a href="/tournaments/<%= user.id%>/<%= tournaments[i].id %>/participants"><button>see participants</button></a>
                </td>
            </tr>
        <%}%>
        </table>
        <%- include('./creditbar.ejs')%>
</body>
</html>